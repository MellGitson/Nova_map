-- NovaMap — Init MySQL
-- Ce fichier s'exécute une seule fois à la création du conteneur.
-- Les migrations Doctrine créent les tables au démarrage de l'app.
SET NAMES utf8mb4;
SET time_zone = '+00:00';
